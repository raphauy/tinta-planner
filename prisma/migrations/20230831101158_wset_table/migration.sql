-- CreateTable
CREATE TABLE "Wset" (
    "id" TEXT NOT NULL,
    "institute" TEXT NOT NULL,
    "location" TEXT,
    "email" TEXT,
    "phone" TEXT,
    "website" TEXT,

    CONSTRAINT "Wset_pkey" PRIMARY KEY ("id")
);
