export type Post= {
    id: string
    title: string
    image: string
    format: string
    hashtags: string
    copy: string
    link: string
    date: string
    pilar: { id: number, name: string, color: string }
}