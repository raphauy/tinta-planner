import { Agency, Client, Pilar, User } from "@prisma/client";

/*
export type FullUserType = User & { 
  agency: Agency; 
};
export type FullClientType = Client & { 
  users: User[]; 
  pilars: Pilar[]
};
*/
